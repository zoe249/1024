---
description: 这是一个 cocos 项目，Creator 版本为 3.8.8
---

## 项目介绍

- 玩法：下落式数字合成——棋子从棋盘上方生成，玩家选择落子列并触发快速下落，落地后同值相邻棋子合并为更大数字。棋盘 5 列 × 7 行，所有列无法放入新棋子时游戏结束。
- 目标平台：微信小游戏（主），Web（预览）。

## 架构约定

- **逻辑与渲染严格分离**：`PlayController` 持有全部游戏状态，通过 `buildUiState()` 产出纯数据 `PlayUIState`，传给 `PlayUIController.renderState()` 渲染。UI 层只能通过回调通知逻辑层，不得直接修改棋盘或游戏状态。
- **棋子是数据也是视图**：每颗棋子对应一个 `PieceController` 组件实例，挂在实例化的 `piece.prefab` 节点上。棋盘数组 `board[row][column]` 存储的是 `PieceController | null`。
- **棋盘坐标**：`row` 从下往上增长（0 = 最底行），`column` 从左往右增长。格子中心坐标由 `BoardFill` 节点尺寸动态计算，不依赖固定像素值。
- **结算流程**：落子 → 同值连通块 BFS → 定向合并（最低行棋子为锚点）→ 受影响列重力下落 → 全盘扫描合并组 → 循环直到无合并。
- **三种技能**：炸弹（点选中心，炸 3×3 范围）、锤子（点选单颗敲碎）、交换（拖拽相邻棋子，形成合并则保留否则回弹）。技能激活时冻结下落，技能动画结束后走统一的全盘结算。

## 代码要求

- 每次修改完代码都需要补充必要注释。
- 注释使用中文。
- 注意注释格式，避免出现乱码。
- 对于复杂度较高的方法，注释使用JSDOC格式

## 注意事项

- 场景目录拼写为 `assets/scence/`（非 "scene"），已被项目引用，不可随意改名。
- 棋盘视觉全部由 `PlayUIController.ensureBoardDecorations()` 用 `Graphics` 代码绘制，不要在场景里手动摆棋盘装饰节点。
- `PlayUIController` 的技能栏节点历史上有拼写别名 `SkliisController`，查找时需同时兼容 `SkillsController`。
- UI 布局优先在层级管理器中调整，只有运行时确实需要动态变化时才用脚本介入。
- 新增功能时不要破坏现有 scene 节点层级和资源引用关系。
- 本项目没有自动化测试，修改逻辑后需要在 Cocos Creator 中手动预览验证。
- 每次完成任务后，以教学方式做简要总结：先说明为什么这么做（设计意图/取舍），再给出关键代码片段。让阅读者能理解思路而不仅是看到结果。
