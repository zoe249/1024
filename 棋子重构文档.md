# 新版棋子重构文档

> 文档状态：实施基线  
> 适用版本：Cocos Creator 3.8.8  
> 目标平台：微信小游戏，Web 预览  
> 实施分支：`codex/piece-refactor`  
> 本文只规定棋子表现层重构，不改变棋盘规则、合并规则和技能结算。

## 1. 重构结论

新版棋子采用“少量共用图片 + 动态颜色 + 动态数字 + 分级装饰”的组合式渲染。

不为 `2、4、8……` 每个数值制作一张完整棋子图片，也不把数字直接烘焙进棋子底图。这样可以同时满足以下要求：

- 保留手绘底板、描边、高光和纸张纹理等设计感。
- 数值和颜色仍可由代码动态更新，支持合并到更高等级。
- 继续适配棋盘根据 `BoardFill` 动态计算出的格子尺寸。
- 避免十几张大尺寸透明 PNG 增加包体和纹理内存。
- 保留现有下落拖尾、合并升级、炸弹和锤子碎片效果。

## 2. 重构目标与非目标

### 2.1 目标

1. 用可复用的 Sprite 图层替换当前纯 `Graphics` 圆角色块。
2. 保留 `PieceController.setValue()` 作为唯一的棋子视图刷新入口。
3. 让 `2` 到 `8192` 的棋子拥有稳定、可读且有层级感的视觉表现。
4. 棋子尺寸变化后，底板、描边、高光、数字、装饰和拖尾自动同步。
5. 控制运行时素材体积，禁止把高清源图和预览图导入 `assets`。
6. 保持 `PlayController`、`BoardModel` 和 `PlayUIController` 的职责边界。

### 2.2 非目标

- 不修改 5 列 × 7 行棋盘结构。
- 不修改 BFS 连通块查找、低位锚点、重力和连锁规则。
- 不修改炸弹、锤子和交换技能的业务规则。
- 不修改 `assets/scence/` 的历史目录拼写。
- 不在本次重构中顺带改造首页、设置页或结算页。
- 不为棋子引入动态远程下载或运行时异步加载。

## 3. 当前实现基线

### 3.1 当前 Prefab

文件：`assets/prefab/piece.prefab`

```text
piece
├─ Value
└─ TrailEmitter
```

当前根节点带有 `Sprite`，但 `PieceController.ensureRoundedBody()` 会将它禁用，并在运行时创建 `Body` 节点，通过 `Graphics.roundRect()` 绘制纯色圆角底板。数字由 `Value/Label` 显示，拖尾由 `TrailEmitter/ParticleSystem2D` 提供。

### 3.2 当前控制器职责

文件：`assets/script/PieceController.ts`

- `setValue(value)`：保存数值并刷新底色、文字颜色和字号。
- `refreshView()`：应用 `PIECE_STYLE` 并刷新完整视图。
- `syncTrailEffect()`：按棋子实际尺寸校准拖尾区域、粒子尺寸和颜色。
- `getSpriteFrame()`：向外部特效提供旧根节点 SpriteFrame。
- `getBackgroundColor()`：向外部特效提供棋子底色。
- `getTextColor()`：向外部提供文字颜色。

### 3.3 与玩法表现的耦合点

文件：`assets/script/PlayController.ts`

1. 新棋子和续局棋子都从 `piece.prefab` 实例化。
2. `PlayController` 会先把根节点 `UITransform` 设置为当前 `pieceSize`，因此子图层不能依赖 Prefab 中固定的 120 像素尺寸。
3. 合并升级通过 `anchor.setValue(nextValue)` 刷新保留棋子。
4. `spawnSkillShatterParticles()` 使用 `getSpriteFrame()` 和 `getBackgroundColor()` 生成技能碎片。
5. `createFxPiece()` 使用相同接口生成合并动画中的临时棋子。

以上接口在迁移期间必须保持可用，否则主棋子虽然显示正常，技能碎片和合并临时棋子仍可能透明、变形或引用旧素材。

## 4. 目标节点结构

```text
piece
├─ TrailEmitter       # 下落拖尾，保持在所有可见图层之后
├─ Shadow             # 共用软阴影，可超出主体少量范围
├─ Body               # 中性白色主体，使用 Sprite.color 动态染色
├─ Outline            # 独立描边，不参与主体染色
├─ Highlight          # 共用柔和高光和轻纹理
├─ Decoration         # 高级棋子的星点、叶片或皇冠，可选
└─ Value              # 动态数字 Label 或 BMFont
```

### 4.1 图层职责

| 节点 | 组件 | 染色 | 说明 |
| --- | --- | --- | --- |
| `TrailEmitter` | `ParticleSystem2D` | 跟随底色 | 保留现有自由坐标拖尾 |
| `Shadow` | `Sprite` | 否 | 所有棋子共用，不要过重 |
| `Body` | `Sprite` | 是 | 唯一承担数值主色的图层 |
| `Outline` | `Sprite` | 否 | 与主体拆开，避免乘色后描边发脏 |
| `Highlight` | `Sprite` | 通常否 | 高光透明度保持克制，不覆盖数字 |
| `Decoration` | `Sprite` | 可选 | 按等级显隐或切换小图 |
| `Value` | `Label`/`BMFont` | 是 | 始终位于所有装饰上方 |

所有 Sprite 使用 `Sprite.SizeMode.CUSTOM`。如果后续棋子允许非正方形尺寸，`Body`、`Outline` 和 `Highlight` 改为 `Sprite.Type.SLICED`；当前正方形格子可先使用普通 Sprite。

## 5. 素材规范

### 5.1 目录

运行时素材统一放入：

```text
assets/images/Pieces/
```

推荐文件：

```text
piece-body-fill.png
piece-outline.png
piece-highlight.png
piece-shadow.png
piece-decoration-sparkle.png
piece-decoration-crown.png
```

高清绘制稿、生成过程图、预览图和提示词继续放在 `design/` 或工作用目录中，不进入 `assets/images/Pieces/`。

### 5.2 尺寸与格式

- 基础图层优先使用 `160×160` 透明 PNG；确认细节足够时可降至 `128×128`。
- 图片四周只保留必要透明空间，不能把很小的主体放在巨大透明画布中央。
- `Body` 使用中性白或浅灰，主体填充区域避免自带明显色相。
- `Outline`、`Highlight` 和 `Shadow` 单独出图，避免 `Sprite.color` 同时污染描边和高光。
- 不允许使用 `1024×1024` 的单棋子图作为运行时素材。
- 不允许为每个数值保存一张“底板 + 数字”的完整 PNG。
- 图集需要保留至少 2 像素间距或挤出边缘，避免缩放时串色。

### 5.3 体积预算

- 单张棋子基础透明图建议控制在 `10–40 KB`。
- 任意单张运行时棋子图片不得超过 `100 KB`，除非有明确的视觉验收理由。
- 棋子基础层、装饰层和数字资源合计目标不超过 `200 KB`。
- `160×160 RGBA` 图片解压后的理论纹理内存约为 100 KB；需要同时控制文件体积和运行时纹理数量。
- 风格稳定后再使用 Cocos 自动图集收拢碎图，不保留手工拼接的大型历史图集。

导入设置应在 Cocos Creator 中完成，不手工编辑 `.meta`。微信小游戏构建时再根据真机兼容性选择纹理压缩格式，并检查透明边缘。

## 6. 数字方案

### 6.1 首选方案：自定义字体 Label

继续使用动态 `Label`，但必须绑定项目内的字体资源，不能依赖微信设备上的系统字体。优点是数值扩展简单、资源少、字号和字距容易动态调整。

### 6.2 备选方案：BMFont

只有普通字体无法达到设计稿的手绘感时，才制作一套仅包含 `0–9` 的 BMFont 图集。数字仍由运行时组合，不为 `128、256、512` 等数值分别出图。

### 6.3 排版规则

- 1–2 位：使用标准字号。
- 3 位：轻微缩小并收紧字距。
- 4 位：继续缩小，确保不触碰描边和装饰。
- 5 位及以上：使用最小安全字号，必要时单独配置 `textScale`。
- 数字区域始终以棋子中心对齐，不使用手工写死的世界坐标。

## 7. 样式数据模型

样式仍由 `PieceController` 集中维护，玩法逻辑只传入数值。建议的数据结构如下：

```ts
type PieceDecoration = 'none' | 'sparkle' | 'crown'

type PieceStyle = {
  bodyColor: string
  textColor?: string
  decoration?: PieceDecoration
  textScale?: number
}

const PIECE_STYLE: Record<number, PieceStyle> = {
  2:    { bodyColor: '#F8ECDC', textColor: '#263745' },
  4:    { bodyColor: '#FFC928' },
  8:    { bodyColor: '#35D6B7' },
  16:   { bodyColor: '#FF5A45' },
  32:   { bodyColor: '#178FD8' },
  64:   { bodyColor: '#7A5CFF' },
  128:  { bodyColor: '#32C85F', decoration: 'sparkle' },
  256:  { bodyColor: '#FF8A1F', decoration: 'sparkle' },
  512:  { bodyColor: '#D947EF', decoration: 'sparkle' },
  1024: { bodyColor: '#F23F8F', decoration: 'crown', textScale: 0.9 }
}
```

正式实现前需根据最终素材确认颜色，并补齐 `2048、4096、8192`。未命中的数值必须使用清晰可见的默认样式，不能出现透明棋子。

分级建议：

- `2–64`：底板、描边、数字和轻高光。
- `128–512`：增加轻量星点或角部纹理。
- `1024` 及以上：允许使用皇冠或光环，但不得遮挡数字。

## 8. PieceController 设计

### 8.1 对外接口

重构后保留以下接口：

```ts
getValue(): number
setValue(value: number): void
syncLayout(force?: boolean): void
syncTrailEffect(force?: boolean): void
stopParticle(): void
getSpriteFrame(): SpriteFrame | null
getBackgroundColor(): Color
getTextColor(): Color
```

其中：

- `setValue()` 仍是唯一数值更新入口。
- `syncLayout()` 统一同步可见图层、数字区域和拖尾尺寸。
- `syncTrailEffect()` 在迁移期保留为兼容入口，内部可转调尺寸同步逻辑，避免遗漏现有调用点。
- `getSpriteFrame()` 改为返回 `Body` 的 SpriteFrame，保证现有技能碎片和临时棋子继续工作。

### 8.2 内部引用

```ts
private bodySprite: Sprite | null = null
private outlineSprite: Sprite | null = null
private highlightSprite: Sprite | null = null
private shadowSprite: Sprite | null = null
private decorationSprite: Sprite | null = null
private valueLabel: Label | null = null
private particleSystem: ParticleSystem2D | null = null
```

节点优先在 Prefab 中建立。`onLoad()` 只解析引用和配置组件，不应重复创建一套可见层级。为避免旧 Prefab 或资源引用暂时缺失导致棋子不可见，迁移阶段可以保留 `Graphics` 兜底；Cocos 预览和真机验证通过后再删除兜底代码。

### 8.3 刷新流程

```ts
onLoad() {
  this.resolveViewNodes()
  this.configureView()
  this.refreshView()
  this.syncLayout(true)
}

setValue(value: number) {
  this.value = value
  this.refreshView()
  this.syncTrailColor()
}

private refreshView() {
  const style = PIECE_STYLE[this.value] ?? DEFAULT_STYLE
  this.currentBgColor = this.fromHex(style.bodyColor)
  this.currentTextColor = this.fromHex(style.textColor ?? '#FFFFFF')

  if (this.bodySprite) {
    this.bodySprite.color = this.currentBgColor
  }

  this.refreshValueLabel(style)
  this.refreshDecoration(style.decoration ?? 'none')
}
```

`refreshView()` 只在初始化或数值变化时执行，不在 `update()` 中每帧重绘，也不为每颗棋子创建独立材质。

### 8.4 尺寸同步

`syncLayout()` 必须读取根节点实时 `UITransform.contentSize`，并同步以下内容：

- `Shadow` 尺寸可为主体的约 `1.04–1.08` 倍。
- `Body`、`Outline` 和 `Highlight` 与根节点尺寸一致。
- `Decoration` 按主体尺寸成比例缩放。
- `Value` 的内容区域、字号和字距根据数字位数计算。
- `TrailEmitter` 的位置、发射范围、粒子尺寸和速度继续按棋子尺寸计算。

不能把 120 像素当成最终显示尺寸；120 只能作为字号缩放的设计基准。

## 9. PlayController 兼容策略

### 9.1 实例化

保持当前流程：

```ts
const pieceNode = instantiate(this.basePieceController)
pieceNode.getComponent(UITransform)?.setContentSize(this.pieceSize, this.pieceSize)
const pieceController = pieceNode.getComponent(PieceController)
pieceController?.setValue(value)
pieceController?.syncLayout(true)
```

需要同时检查普通生成与续局恢复两个入口，不能只改 `spawnPiece()`。

### 9.2 技能碎片

`spawnSkillShatterParticles()` 暂时继续使用：

```ts
const spriteFrame = piece.getSpriteFrame()
const baseColor = piece.getBackgroundColor()
```

重构后 `getSpriteFrame()` 返回 `Body` 图层的 SpriteFrame。碎片无需复制文字、高光和皇冠，保持单层、轻量即可。

### 9.3 合并临时棋子

`createFxPiece()` 当前只复制 SpriteFrame 与底色。第一阶段保持这一行为，避免扩大改动范围。如果最终视觉验收发现临时棋子与主体差异明显，再单独增加由 `PieceController` 创建视觉快照的接口，不在第一阶段提前抽象。

### 9.4 合并升级

`animateAnchorUpgrade()` 仍调用 `anchor.setValue(nextValue)`，因此颜色、数字、装饰和拖尾颜色必须在同一次刷新中完成，避免动画过程中先变数字后变颜色。

## 10. 实施步骤

### 阶段 A：建立视觉基线

1. 在 Cocos Creator 3.8.8 记录当前 `2、16、128、1024` 棋子的 Web 预览截图。
2. 记录棋盘实际 `pieceSize`、棋子间距和数字最大安全区域。
3. 使用 `design/references/` 下的棋子图作为风格参考，不直接切图作为正式素材。

### 阶段 B：制作运行时素材

1. 生成或绘制中性白主体、独立描边、高光、阴影和装饰。
2. 裁切透明边缘并缩放到 160×160 或更小。
3. 检查染成当前全部棋子颜色后是否出现脏边、偏色和高光过曝。
4. 压缩后放入 `assets/images/Pieces/`，让 Cocos 自动生成 `.meta`。

### 阶段 C：调整 Prefab

1. 在层级管理器中为 `piece.prefab` 增加目标节点。
2. 保留根节点名称、`Value`、`TrailEmitter` 和 `PieceController`。
3. 配置 SpriteFrame、节点层级、锚点和 `Sprite.SizeMode.CUSTOM`。
4. 不改变 Prefab 的外部引用和根节点语义。

### 阶段 D：改造 PieceController

1. 扩展 `PieceStyle`。
2. 解析新图层引用。
3. 将主体颜色写入 `Body/Sprite.color`。
4. 实现装饰显隐、数字排版和统一尺寸同步。
5. 让 `getSpriteFrame()` 返回 `Body` SpriteFrame。
6. 保留缺图时的可见兜底，完成验证后再移除旧 `Graphics` 绘制路径和 `ORIGINAL_PIECE_STYLE` 备份。

### 阶段 E：兼容特效

1. 验证下落拖尾位置和颜色。
2. 验证锤子、炸弹碎片能够读取新主体图。
3. 验证合并临时棋子和升级动画没有透明、闪色或尺寸跳变。
4. 仅在出现明确差异时修改 `PlayController` 的视觉辅助方法，不改结算逻辑。

### 阶段 F：清理与打包

1. 删除不再使用的旧棋子运行时图片和临时兜底代码。
2. 通过 Cocos 资源引用检查确认没有丢失 SpriteFrame。
3. 风格稳定后建立小型自动图集。
4. 检查微信小游戏构建产物中的素材体积和纹理数量。

## 11. 文件改动范围

| 文件或目录 | 预期改动 |
| --- | --- |
| `assets/images/Pieces/` | 新增压缩后的运行时棋子分层素材 |
| `assets/prefab/piece.prefab` | 增加并配置可见图层节点 |
| `assets/script/PieceController.ts` | 替换表现实现、扩展样式和尺寸同步 |
| `assets/script/PlayController.ts` | 仅做必要的尺寸同步与特效兼容调整 |
| `design/references/` | 只作为视觉参考，不建立运行时引用 |
| `.meta` 文件 | 由 Cocos 自动维护，不手工修改 |

原则上不应修改 `BoardModel`、合并搜索、重力流程、技能状态机或 UI 状态数据结构。

## 12. 验证清单

### 12.1 基础显示

- [ ] `2、4、8、16、32、64、128、256、512、1024` 显示正确。
- [ ] `2048、4096、8192` 有明确样式，不落入不可读颜色。
- [ ] 三位、四位和五位数字没有溢出、裁切或偏心。
- [ ] 高光不过曝，描边不随主体染色发脏。
- [ ] 装饰不遮挡数字，不改变棋子实际布局尺寸。

### 12.2 棋盘与输入

- [ ] 新棋子正常生成在可落子列。
- [ ] 拖动选列和点击快速下落正常。
- [ ] 棋子落地位置与 5×7 网格严格对齐。
- [ ] 不同预览尺寸下，所有图层仍跟随动态 `pieceSize`。

### 12.3 合并与重力

- [ ] 相邻同值棋子能正常合并。
- [ ] 低位锚点规则不变。
- [ ] 合并后数值、颜色、装饰和拖尾颜色同步刷新。
- [ ] 多段连锁和全盘扫描正常。
- [ ] 重力下落后没有残留旧图层或错误层级。

### 12.4 技能与状态

- [ ] 炸弹、锤子和交换技能正常。
- [ ] 技能期间普通下落仍被冻结。
- [ ] 炸弹和锤子碎片使用新主体图片与正确颜色。
- [ ] 暂停、恢复、返回首页、重新开始和游戏结束流程正常。
- [ ] 续局恢复的棋子与新生成棋子表现一致。

### 12.5 平台与性能

- [ ] Web 预览无报错、无缺图、无透明黑边。
- [ ] 微信开发者工具和真机字体一致。
- [ ] 真机透明边缘无白边、黑边或图集串色。
- [ ] 不在每帧创建节点、材质、颜色表或 SpriteFrame。
- [ ] 运行时棋子素材符合本文件体积预算。

## 13. 回滚策略

重构应分阶段提交：素材、Prefab、控制器和清理分别形成可识别的提交。迁移阶段保留缺图时的 `Graphics` 兜底，若真机出现兼容问题，可先恢复旧表现而不回滚玩法代码。

回滚不得使用会覆盖其他改动的破坏性命令。需要回退时优先对相关提交执行正常的 Git revert，或只恢复本文件改动范围内的节点和脚本。

## 14. 完成定义

满足以下条件才算完成棋子重构：

1. 新版棋子已使用分层小图，而不是每个数值一张完整图片。
2. `PieceController` 独立负责棋子视觉，玩法控制器不直接操作内部图层。
3. 所有动态尺寸、合并、重力、技能、暂停和续局检查通过。
4. Cocos Creator 3.8.8 中资源引用正常，Web 与微信真机表现可接受。
5. 旧运行时素材和迁移兜底已在验证后清理，包体没有因新版棋子明显增长。

## 15. 视觉参考

- `design/references/design_tiles_2_to_8192_complete_v3.png`
- `design/references/design_tiles_2_to_8192_with_frozen_states_v2.png`
- `design/references/tile_frozen_overlay_240.png`

这些图片只用于确认风格、颜色层级和冻结状态。正式运行时素材必须按本文的分层、裁切和压缩规则重新制作。
